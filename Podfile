# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'backendless_server' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Backendless'
  # Pods for backendless_server

  target 'backendless_serverTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'backendless_serverUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
